import React from "react";

import { Box } from "@mui/material";

export const Landing = () => {
  return (
    <Box
      sx={{
        minHeight: "100dvh",
        display: "flex",
        flexDirection: "column",
        backgroundColor: "background.default",
      }}
    >
      <h1>Hi from landing page, wow</h1>
      {/* Hero con logo + titolo + tagline */}
      {/* Input email */}
      {/* Bottone "Enviar código de acceso */}
      {/* Divisore "o" */}
      {/* Bottone Google */}
      {/* Nota in fondo */}
    </Box>
  );
};
